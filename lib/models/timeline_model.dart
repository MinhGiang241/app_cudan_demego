import 'package:flutter/material.dart';

class TimelineModel {
  TimelineModel({this.date, this.id, this.title, this.subTitle, this.color});
  String? id;
  String? date;
  String? title;
  String? subTitle;
  Color? color;
}
